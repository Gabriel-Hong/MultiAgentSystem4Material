{{- if .Values.monitoring.grafana.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  password: {{ .Values.monitoring.grafana.adminPassword | b64enc }}
{{- end }}

